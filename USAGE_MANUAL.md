# easyDataScience 使い方マニュアル

このマニュアルは、easyDataScience を使ってデータの確認、前処理、回帰・分類 AutoML、画像分類、音声分類、保存済みモデルによる予測を行うための利用者向けガイドです。

easyDataScience は、PyCaret のような機械学習ワークフローをブラウザ上で体験できる教育用アプリです。CSV / Excel の表データだけでなく、画像分類と音声分類も扱えます。通常の読み込み、前処理、学習、評価、予測はブラウザ内で行われます。

生成AI支援では、APIキーがなくても分析結果ページの「AI用テキストをコピー」を使って、ChatGPT、Gemini、Claudeなどの外部生成AIへ貼り付けるための文脈をコピーできます。コピー時はネットワーク送信しませんが、内容はシステムのクリップボードへ保存されます。Gemini APIキーを入力すると、分析結果ページで「解釈を生成」したり、結果について追加質問したりできます。送信前にはパネルで実際の文脈を確認し、個人情報・機密情報を含まないことへのチェックが必要です。この操作をした場合だけ、要約統計量、データ構造、分析手法、主要指標、注意点などの分析文脈が Google Gemini API へ送信されます。先頭10件のプレビューは利用者が明示的に許可した場合だけ含まれますが、列名・クラス名・要約統計・分析結果はプレビュー設定にかかわらず送信対象です。APIキーと追加質問の履歴はJavaScriptのメモリ内にだけ保持され、再読み込みまたはページを閉じると消えます。Interactions APIへの全送信で`store=false`を指定し、API側の会話履歴保存を要求しません。生成内容は元の結果と照合してください。

Googleは2026年9月からGemini APIのStandard keyを拒否します。[公式案内](https://ai.google.dev/gemini-api/docs/api-key)に従い、Google AI Studioで作成したAuth keyを使用してください。

既定モデルは2026年8月時点の安定版`gemini-3.7-flash`です。解釈は6節の構造化JSONとして受信し、型、項目数、文字数を検証してから表示します。回答下部には実際に使われたモデルと総トークン数を表示します。指定モデルが利用できない場合だけ、Interactions APIと構造化出力に対応する安定版`gemini-3.6-flash`へ切り替わり、その事実も表示されます。

## 1. できること

easyDataScience では、次の機能を利用できます。

- 機械学習入門ガイド
- CSV / Excel データの読み込み
- データプレビューと要約統計量の確認
- 探索的データ分析（EDA）
- データ前処理
- 回帰モデル比較（AutoML）
- 分類モデル比較（AutoML）
- ハイパーパラメータチューニング
- モデル解釈（特徴量重要度、PDP、学習曲線、SHAP 系の可視化）
- ブレンド、スタッキング、ファイナライズ
- 新しいデータでの予測
- 比較結果・予測結果の CSV ダウンロード
- 学習済みモデルの JSON エクスポート
- JSON モデルの読み込みと再予測
- MobileNet 転移学習による画像分類
- Web Audio API を使った音声分類

## 2. 起動方法

### ブラウザで使う場合

公開ページがある場合は、ブラウザで easyDataScience のページを開くだけで使えます。インストールは不要です。

### 表示言語を切り替える

ページ上部の `JP / EN` で、操作画面、分析結果の説明、学習ガイド、マニュアル、生成AI支援を再読み込みなしで切り替えられます。選択した言語はブラウザに保存され、アプリとマニュアルで共有されます。

列名、ファイル名、クラス名、入力値などのユーザーデータは翻訳されません。

### ローカルで使う場合

リポジトリを手元に置いて使う場合は、プロジェクトルートでローカルサーバーを起動します。

```bash
python3 -m http.server 8765
```

ブラウザで次のURLを開きます。

```text
http://localhost:8765
```

`index.html` を直接開くと、ブラウザのセキュリティ制限で ES Modules やデモデータの読み込みが失敗することがあります。ローカルサーバー経由で開くのがおすすめです。

## 3. 基本操作の流れ

表データを使う分析は、次の順番で進めます。

1. データをアップロードする、またはデモデータを読み込む
2. データプレビューと要約統計量を確認する
3. 必要に応じて EDA で分布、相関、欠損値を確認する
4. 必要に応じてデータ前処理を確認する
5. 目的変数の種類に合わせて「回帰」または「分類」を選ぶ
6. 目的変数、テストデータ割合、交差検証Fold数、特徴量を設定する
7. モデル比較を開始する
8. 前処理込み交差検証の比較表から候補モデルを確認する
9. 必要に応じてチューニングし、最終候補を決める
10. 未使用テストデータを一度だけ開示して最終評価する
11. 必要に応じて解釈、ブレンド、スタッキング、ファイナライズを行う。開示後の比較は探索的に扱う
12. predict_model で新しいデータの予測を試す
13. CSV または JSON で結果やモデルを保存する

## 4. データ準備のルール

機械学習では、データの形が結果の信頼性に直結します。読み込み前に、次の形に整えてください。

### 正しい表の例

| 面積 | 築年数 | 駅距離 | 部屋数 | 価格 |
|---:|---:|---:|---:|---:|
| 65 | 12 | 8 | 3 | 4200 |
| 80 | 5 | 4 | 4 | 6100 |
| 45 | 25 | 15 | 2 | 2800 |

守ること:

- 1行目は列名だけにする
- 2行目以降にデータを入れる
- 1件のデータを1行にする
- セル結合を使わない
- 数値列に `万円`、`km`、`約` などの文字を混ぜない
- 欠損値の扱いを分析前に確認する
- 目的変数を1つ決めておく

### 避けるべき表の例

| 住宅価格データ |  |  |
|---|---|---|
| 面積 | 築年数 | 価格 |
| 約65㎡ | 12年 | 4200万円 |

この形は、見出し行が複数ある、単位が混ざっている、機械が数値として読み取りにくい、という問題があります。

## 5. データ型の考え方

### 数値変数

足し算、平均、大小比較に意味がある列です。

例:

- 価格
- 売上
- 年齢
- 面積
- 学習時間
- テスト点数

数値を予測したい場合は、回帰を使います。

### カテゴリ変数

グループ名やラベルを表す列です。

例:

- 解約する / しない
- 品種
- 性別
- クラス
- 合格 / 不合格

カテゴリを予測したい場合は、分類を使います。

### 画像・音声データ

画像分類と音声分類では、CSVではなく画像ファイルや音声サンプルを直接使います。表データのアップロードは不要です。

## 6. デモデータ

手元にデータがない場合は、「デモデータを試す」からサンプルデータを読み込めます。

### 回帰向け

- `regression_demo.csv`: 住宅価格データ。面積や築年数から価格を予測します。
- `tips.csv`: 食事の合計金額などからチップを予測します。

### 分類向け

- `classification_demo.csv`: 顧客の解約を予測します。
- `iris.csv`: 花の計測値から品種を分類します。
- `wine.csv`: 化学分析値からワイン品種を分類します。
- `penguins.csv`: 体の計測値から種類を分類します。

初めて使う場合は、回帰なら `regression_demo.csv`、分類なら `classification_demo.csv` から始めると操作を理解しやすいです。

## 7. 機能別ガイド

### 機械学習入門ガイド

データを読み込まなくても使える学習コンテンツです。教師あり学習、回帰と分類、評価指標、特徴量と前処理、モデル比較などを図解付きで確認できます。

使う場面:

- 機械学習の基本用語を確認したい
- 回帰と分類の違いを学びたい
- R2、MAE、Accuracy、F1 などの指標を復習したい
- アンサンブル学習や過学習のイメージをつかみたい

### 探索的データ分析（EDA）

データの分布、相関、欠損値を可視化します。

必要なデータ:

- 数値変数が1つ以上

見るポイント:

- 極端な外れ値がないか
- 欠損が多い列はないか
- 目的変数と関係しそうな特徴量はあるか
- 数値変数同士の相関が強すぎないか

回帰や分類に進む前に、まず EDA を見ると、モデルの失敗原因を早めに発見できます。

### データ前処理

欠損値補完、スケーリング、エンコーディングなど、機械学習に必要な前処理を確認する機能です。

見るポイント:

- 欠損値がどの列にあるか
- 外れ値が多すぎないか
- 標準化や正規化が必要そうか
- カテゴリ変数を数値化する必要があるか

AutoML の回帰・分類画面では、学習時にも自動前処理が実行されます。

### 回帰モデル比較（AutoML）

数値を予測したいときに使います。

例:

- 住宅価格を予測する
- 売上を予測する
- テスト点数を予測する
- チップ金額を予測する

必要なデータ:

- 目的変数にしたい数値列が1つ
- 特徴量として使う列が1つ以上

操作手順:

1. 「回帰モデル比較（AutoML）」を開く
2. 目的変数を選ぶ
3. 無作為・グループ非重複・時系列から、データ構造に合う分割方法を選ぶ
4. テストデータ割合と交差検証 Fold 数を選ぶ
5. 使用する特徴量を確認する
6. 「モデル比較を開始」を押す
7. 前処理込みCV R2の平均とfold間SDで候補を選ぶ
8. 最終候補を決めてからテストデータを開示し、R2、MAE、RMSEとブートストラップ区間を確認する

搭載モデル:

- 線形回帰
- Ridge回帰
- Lasso回帰
- 決定木
- ランダムフォレスト
- K近傍法
- 勾配ブースティング

主な評価指標:

- R2: 0は訓練データ平均による予測と同程度、負値はそれを下回る。十分かどうかは分野とベースラインで判断する
- MAE: 予測値と実測値の平均的なずれ
- RMSE: 大きな誤差を重く見るずれ
- CV R2: 各foldで前処理を訓練foldだけにfitした交差検証の平均。fold間SDは信頼区間ではありません。

### 分類モデル比較（AutoML）

カテゴリを予測したいときに使います。

例:

- 解約する / しないを予測する
- 品種を分類する
- 合格 / 不合格を予測する
- 顧客タイプを分類する

必要なデータ:

- 目的変数にしたいカテゴリ列が1つ
- 特徴量として使う列が1つ以上（数値・カテゴリの両方に対応）

操作手順:

1. 「分類モデル比較（AutoML）」を開く
2. 目的変数を選ぶ
3. 二値分類ではAverage Precisionの陽性クラスを確認する
4. 無作為層化・グループ非重複・時系列から、データ構造に合う分割方法を選ぶ
5. テストデータ割合と交差検証 Fold 数を選ぶ
6. 使用する特徴量を確認する
7. 「モデル比較を開始」を押す
8. 前処理込みCV Macro F1の平均とfold間SDで候補を選ぶ
9. 最終候補を決めてからテストデータを開示し、Macro F1、Accuracy、MCC、クラス別指標、AUC、Average Precision、混同行列を確認する

搭載モデル:

- ロジスティック回帰
- 決定木
- ランダムフォレスト
- K近傍法
- ナイーブベイズ
- SVM
- 勾配ブースティング

主な評価指標:

- Accuracy: 全体の正解率
- Macro Precision: クラスごとのPrecisionを同じ重みで平均した値
- Macro Recall: クラスごとのRecallを同じ重みで平均した値
- Macro F1: クラスごとのF1を同じ重みで平均した値
- AUC: 確率スコアの順位付けによる識別性能。確率の校正は示さない
- Average Precision: 指定した陽性クラスのPrecisionとRecallをしきい値横断で要約する二値分類指標
- MCC: 混同行列全体を使う-1〜1の指標。クラス不均衡時にもAccuracyを補完する
- LogLoss: 予測確率の外れ具合
- CV Macro F1: 各foldで前処理を訓練foldだけにfitした交差検証の平均。fold間SDは信頼区間ではありません。

クラス不均衡がある場合は、Accuracy だけでなく F1、Precision、Recall を必ず確認してください。

## 8. AutoML ワークフローの読み方

回帰・分類の画面は、PyCaret 風に次の流れで進みます。

### Setup

目的変数、テストデータ割合、交差検証 Fold 数、特徴量を決めます。

### Preprocess

データ分割、欠損処理、エンコーディング、スケーリングなどが自動で実行されます。

### Compare

テストデータを封印したまま、複数モデルを前処理込み交差検証で比較します。

### Create

比較結果から選ばれたモデルを、詳細評価や後続処理で使える状態にします。

### Tune

候補モデルのハイパーパラメータを探索します。改善した場合は、チューニング後のモデルを以降の予測に利用できます。

### Interpret

モデルの中身を確認します。

主な表示:

- 特徴量重要度
- Permutation Importance
- PDP（Partial Dependence Plot）
- 学習曲線
- SHAP 系の可視化

### Blend

上位モデルの予測を平均または統合して、より安定した予測を目指します。

### Stack

上位モデルの予測をメタモデルに入力し、さらに学習して予測を行います。

### Finalize

選択したモデルを最終モデルとして確定し、JSON として保存できるようにします。

### Predict

新しい特徴量を入力し、学習済みモデルで予測します。結果は CSV としてダウンロードできます。

## 9. モデル保存と予測モード

回帰・分類でファイナライズしたモデルは、JSONファイルとして保存できます。

保存したモデルを使うには、トップ画面の「モデル読み込み＆予測」を開きます。

操作手順:

1. 「モデル読み込み＆予測」を開く
2. 保存済みの JSON モデルファイルをアップロードする
3. モデル名、タスク、目的変数、特徴量数を確認する
4. 表示された入力フォームに新しい特徴量を入力する
5. 「予測を実行」を押す

回帰モデルでは数値の予測結果が表示されます。分類モデルでは予測クラスと、対応モデルではクラス別確率が表示されます。

## 10. 画像分類（MobileNet）

画像分類では、CSVデータを使わず、画像ファイルを直接アップロードしてモデルを作ります。MobileNet の転移学習を使います。

必要なデータ:

- 2クラス以上
- 各クラス最低2枚以上の画像
- JPEG、PNG、WebP 形式の画像
- 1枚15 MiB以下、各クラス200枚まで

操作手順:

1. 「画像分類（MobileNet）」を開く
2. クラスを追加する
3. 各クラスに画像をアップロードする
4. エポック数と検証データ割合を選ぶ
5. 学習を実行する
6. 層化された未学習データの評価結果、学習曲線、精度曲線、クラス別再現率、混同行列を確認する
7. 必要に応じて PCA、検証データの予測スコア、注目領域の可視化を確認する
8. 新しい画像をアップロードして分類する

注意:

- 画像枚数が少ないと、評価結果は不安定になります。
- 背景や撮影条件がクラスごとに偏ると、モデルが本来の対象ではなく背景を覚えることがあります。
- 教育・体験用途では、まず少数クラスで動作を確認し、その後画像数を増やしてください。
- 画面の検証データは学習曲線や設定確認にも使うため、最終的な独立テストではありません。別の撮影条件で再評価してください。

## 11. 音声分類（Web Audio）

音声分類では、マイク録音または音声ファイルからサンプルを集め、スペクトル特徴量を使って分類モデルを学習します。

必要なデータ:

- 2クラス以上
- 各クラス2つ以上の音声サンプル
- 3秒程度のクリップ
- アップロードは1件25 MiB・30秒以下、各クラス100件まで

操作手順:

1. 「音声分類（Web Audio）」を開く
2. クラス名を入力して追加する
3. 各クラスで録音または音声サンプルをアップロードする
4. 学習を開始する
5. 層化された未学習データの評価結果、クラス別再現率、混同行列を確認する
6. 新しい音声を録音またはアップロードして分類する

注意:

- マイク利用時はブラウザのマイク許可が必要です。
- 周囲の雑音が多いと精度が落ちます。
- 学習サンプルと予測サンプルの録音条件が大きく違うと、結果が不安定になります。
- 画面の検証データは最終的な独立テストではありません。別の話者・機器・環境でも再評価してください。

## 12. 動画・画像分析

動画・画像分析では、ローカルの動画または静止画をブラウザ内で観察し、描画、タグ、2画面比較、MoveNetによるポーズ推定を行えます。

1. 動画または画像を読み込み、再生・速度・コマ送りで観察する
2. 線や図形を描き、動画では表示時刻と秒数を設定する
3. 動画の出来事をタグで記録し、CSVへ書き出す
4. 必要に応じて2つの動画を並べ、オフセット付きで同期比較する
5. MoveNetで骨格と関節角度を推定し、結果と限界を確認する

メディアは外部送信されません。アノテーションJSONは5 MiB以下で、読込時に形式と件数を検証します。ポーズ推定は遮蔽、画角、衣服、複数人により誤ることがあります。

## 13. 結果を見るときの注意

### テストデータは候補確定後に一度だけ開示する

モデル選択とチューニングは交差検証で行い、候補を決めてから未使用テストデータを開示します。開示後にモデルや構成を選び直した場合、そのテスト結果は独立評価ではなく探索的な参考値です。

### 指標を1つだけで判断しない

回帰では R2 だけでなく MAE と RMSE も確認してください。分類では Accuracy だけでなく F1、Precision、Recall、混同行列も確認してください。

回帰の新規入力に表示される値は点予測です。個別の将来観測に対する予測区間ではありません。

### 特徴量重要度を過信しない

特徴量重要度は、モデルが何を手がかりにしたかを見るためのヒントです。因果関係を証明するものではありません。

### データの前提を確認する

外れ値、欠損、カテゴリの表記ゆれ、クラス不均衡があると、モデル性能や解釈に大きく影響します。

カテゴリ特徴量は、保持データや検証foldを見ずに各訓練区画でOne-Hot Encodingされます。検証・予測時の未知カテゴリは全0になります。未知カテゴリが多い場合は、カテゴリ表記やデータ収集範囲を見直してください。

無作為分割は、各行が独立同分布で将来データも同じ分布に従うことを仮定します。時系列では時系列分割、同一人物の反復測定や学校・施設などの構造ではグループ非重複分割を選び、必要に応じて時系列gapを設定してください。

## 14. よくあるトラブル

### ファイルが読み込めない

確認すること:

- 拡張子が `.csv`, `.xlsx`, `.xls` のいずれかになっているか
- 1行目が列名になっているか
- 空の行や列が大量に含まれていないか
- セル結合や装飾用の見出し行がないか

### 分析カードが押せない

カードごとに必要なデータ条件があります。

- EDA: 数値変数が1つ以上
- データ前処理: 数値変数が1つ以上
- 回帰: 数値変数が2つ以上
- 分類: 数値変数が1つ以上
- 画像分類、音声分類、予測モード: CSVデータなしで利用可能

### 数値列が特徴量として使えない

列のどこかに文字が混ざっている可能性があります。

修正例:

- `80点` を `80` にする
- `約60` を `60` にする
- `1,000` のカンマを取り除く
- 全角数字を半角数字にする
- 空欄や欠損表記を統一する

### 分類の精度が高いのに実用性が低い

クラス不均衡がある場合、Accuracy が高くても少数クラスをほとんど検出できていないことがあります。F1、Precision、Recall、混同行列を確認してください。

### 画像・音声分類がうまく学習しない

確認すること:

- 各クラスのサンプル数が十分か
- クラスごとのサンプル数が極端に偏っていないか
- 学習データと予測データの撮影・録音条件が近いか
- 背景や雑音がクラス判定の手がかりになっていないか

## 15. 分析前チェックリスト

- [ ] 目的変数を決めた
- [ ] 目的変数が数値なら回帰、カテゴリなら分類を選ぶと決めた
- [ ] 1行目が列名だけになっている
- [ ] セル結合や装飾用の見出し行がない
- [ ] 数値列に単位や文字が混ざっていない
- [ ] 欠損値と外れ値を確認した
- [ ] クラス不均衡を確認した
- [ ] モデル選択前にテストデータを開示しない
- [ ] 時系列・反復測定・グループ構造の有無を確認した
- [ ] 指標だけでなくグラフや混同行列も見る
- [ ] モデル保存が必要な場合はファイナライズ後に JSON をダウンロードする

## 16. 授業・研修でのおすすめ順

初学者に使ってもらう場合は、次の順番がおすすめです。

1. 機械学習入門ガイドを見る
2. デモデータを読み込む
3. EDA でデータを見る
4. 回帰モデル比較で数値予測を体験する
5. 分類モデル比較でカテゴリ予測を体験する
6. チューニングとモデル解釈を確認する
7. ファイナライズして JSON モデルを保存する
8. 予測モードで保存済みモデルを読み込む
9. 画像分類または音声分類を体験する

## 17. 参考ファイル

- `index.html`: アプリの入口とトップ画面
- `js/main.js`: ファイル読み込み、デモデータ、機能カード制御
- `js/analyses/learning_guide.js`: 機械学習入門ガイド
- `js/analyses/eda.js`: 探索的データ分析
- `js/analyses/preprocessing.js`: データ前処理画面
- `js/analyses/regression.js`: 回帰 AutoML
- `js/analyses/classification.js`: 分類 AutoML
- `js/analyses/image_classification.js`: 画像分類
- `js/analyses/audio_classification.js`: 音声分類
- `js/analyses/prediction_mode.js`: JSONモデル読み込み予測
- `datasets/`: デモデータ
- `js/ml/`: 機械学習・前処理・評価指標の実装

---

最終更新: 2026-08-29
